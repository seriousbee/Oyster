package com.tfl.billing;

public class TestUtil {
/*
    DateTime startTime;
    Random random;

    public TestUtil(){
        random = new Random();
    }


    public DateTime generateRandomPeakStartTime(){
        while(startTime==null){
            startTime.hourOfDay().setCopy(random.nextInt()%24);
            if(!JourneyCostCalculator.isPeak(startTime.toDate()))
                startTime = null;
        }
        return startTime;
    }

    public DateTime generateRandomPeakEndTime(){

        while(startTime==null){
            startTime.hourOfDay().setCopy(random.nextInt()%24);
            if(!JourneyCostCalculator.isPeak(startTime.toDate()))
                startTime = null;
        }
        return startTime;
    }*/
}
